> 📌 **Andy: keep answers short — 1–4 paragraphs** (not too short, not too long). He asks for details if needed. <!-- ANDY-PREF -->

# MDS Scorecard — Next-Session Plan (updated 2026-07-20)

> **Read this first.** Source of truth = ClickUp `2531q-100317` (Scorecard) + `2531q-100937` (Tools Health). Session log = `SESSION_LOG.md` (2026-07-08 entry = the FB conversation-digest scraper redesign — read it). Tooling = `/Users/Born/mds-scorecard-tools/` (extension + `process_fb.py` + `reconcile.py` + `auto_import.py`; `config.json` holds the AT PAT + Slack token, local only). Airtable via **curl** (MCP auth is bad). Extension reload = **Remove + Load unpacked** (MV3). Scorecard base `appUM1F29IJsMsXRb`.

## 🔝 ACTIVE — FB conversation-digest scraper, extension **v0.66** (READ the `SESSION_LOG.md` 2026-07-20 entry — it supersedes the steps below)
State: **1-day validation PASSED 2026-07-21** — v0.64 Featured-skip is now STRUCTURAL (bank only inside `[role="feed"]`; featured sits outside it — proven via live console dump, no more heuristics), v0.65 added the **chronological kill-switch** (`window-edge` stop: once loaded feed passes the window edge, unreached banked posts are shelled ABOVE → stop; bottom-jumps disabled past the edge — fixed the "scrolled to Jul 15" dive). Clean run `mds_feed (15).json`: 8 posts, `stoppedReason:"window-edge"`, `partial:false` (first fully self-terminated run), 10-min comment freshness, no replies, no featured. v0.66 = `CONV_DAYS` 1→4 only. Monitoring LIVE (heartbeat + quality gate + capture-OK), ghost report fixed at the root, roster "Added by" fix + retry. Reload wipes the Weekly toggle → **re-enable it**. Debug protocol: claude-in-chrome attach to Andy's real Chrome (his session, no new fingerprint) — no more screenshot-and-guess. Immediate next, in order:
0. ~~One 4-day run on v0.66~~ **DONE ×2 — VALIDATED AT CADENCE (read `SESSION_LOG.md` 2026-07-23 Scorecard entry):** Mon 4-day (18/18, 21 posts/51 cmts) + Thu 4-day (39/39, 40 posts/139 cmts, `mds_feed (17).json`). Both Monday misses (Aaron Fuhrman OPENAI + Richard Laatz — FB-side chronological-listing omissions, proven) **self-healed via the Thu overlap**; gap-filler PARKED unless misses recur. Freeze holds — next runs are production Mon/Thu (manual button, tab frontmost).
2. ~~Apply schema + build loader~~ **DONE 2026-07-23** — `digest.fb_posts/fb_comments/fb_member_map` + view `digest.fb_activity` live (migration `fb_digest_store`); `load_feed.py` loads Mon+Thu files (49 posts/160 cmts/0 orphans, 93% member-matched). **Run `python3 mds-scorecard-tools/load_feed.py` after every capture.** Open: 3 members' empty `FB Profile Link` in Members DB (Tamkin/Matthew/Ivan — Ivan has 2 FB accounts, ask Andy).
3. **🎯 FULL-2026 ARCHIVE — ENUMERATION DONE 2026-07-23** (read SESSION_LOG top). Winner after Apify/export/search all failed = **human manual scroll + GraphQL-capture snippet** → 2,085 posts (all 2026, 100% author uid) → `load_manual.py` → `digest.fb_posts` = **2,093 posts, 92% member-resolved.** REMAINING: (a) **fix the snippet's text extraction** (`message.text` path wrong → text 0/2085) + **bake it into the extension as a paste-proof "Manual Capture" button** (FB blocks console-paste; reload loses scroll → inject via extension); (b) **PASS-2 comments — PIPELINE PROVEN + full run LAUNCHED 2026-07-23 eve** (`dz_omar/facebook-comment-scraper` = `K5EXlxalV2BCYfgKM`; `load_comments.py`→`fb_comments`; $0.50/1k comments; 160 test comments loaded member-resolved). Run `UhxU1fVDNNAwo8O6S` (1,928 URLs) RUNNING — **when it finishes: load it, resume any posts still missing comments** (I drive via `apify_fb_run.py resurrect/status/pull` — no cookies → harness allows). See SESSION_LOG top for gotchas (Text-file upload=0 urls, 256MB is fine, burner FB-flag). Ground truth = FB Insights "Daily numbers" (2,229 posts / 17,957 comments). `.apify_token` re-added; Andy's disposable 2nd FB acct. ↓ *(old approach below is SUPERSEDED)*
   **🎯 ANDY'S STANDING GOAL (2026-07-23): the FULL-2026 FB archive — "skipping is not an option."** Missing: Jan 1 → Jun 11 (~5 months, est. 1,200–1,500 posts) + Jul 1–6 + Jul 10–15. **KEY UNLOCK: Andy has a SECONDARY FB account, already a group member, that he doesn't care to lose** → removes the ban-risk that parked every aggressive method. Routes recon'd 2026-07-23: feed deep-scroll DEAD (shells past ~3wk, any account); **mbasic.facebook.com DEAD** (FB redirects it to www); **GraphQL replay DROPPED** (brittle doc_ids/tokens, highest detection — Andy skeptical, correctly). **RECON DONE 2026-07-23 (live, main account) — search DISPROVEN as an enumerator:** FB group search + date filter IS custom-range to the day and URL-constructable (`filters=` base64 of `{rp_creation_time:0 → {name:creation_time, args:{start_day,end_day,...}}}`), BUT it **relevance-ranks and caps**: same Jul 20–21 window (~15 real posts) returned only 5 for `q=the`, 3 for `q=a`, DIFFERENT subsets — no single query is complete, query-union never proves completeness. Good for LOOKUP (find a known post — reaches 2023), useless for "every post." So all 3 free routes fail exhaustive enumeration: feed-scroll shells >3wk, mbasic dead, search capped. **ONLY viable path = Apify maintained FB-group actor + burner cookies.** BLOCKERS (only Andy can clear): (1) **reconnect the Apify connector** — token INVALID, none in any project file (grepped); (2) burner's FB cookie pasted into the actor input by Andy (credential boundary — Claude doesn't handle it); (3) small $; our old custom actor `sSX1L7hnaohLSWTdB` capped ~390 → use a maintained actor, UNPROVEN at 6-mo depth for our private group. **Plan once unblocked: vet best-rated FB-group actor → JUNE test on burner (densest existing data = ground truth) → run Jan–Jun.** The day-precise search-filter URL is still worth keeping for targeted lookups/audits. Never deep/aggressive-run the MAIN account.
4. **Olivia hookup (STAGED — do as an OLIVIA session):** derive `digest.content_items` rows (source `fb_post`/`fb_comment`) so `content_search()` finds FB; **extend `scripts/olivia_leak_gate.py` FIRST**, gate GREEN before ship; decide sensitivity posture (Aytac thread). Then: **summary job** (reuse WA-digest pipeline vs standalone).

**Cadence decision (Andy):** move to **Mon + Thu** (~4-day windows). Overlap is SAFE by design (upsert on stable ids; counts derived).

## Below = older 2026-07-06 plan (scorecard scoring). Task 1 (ghost card) DONE 2026-07-08; tasks 2-5 still open.
- **Task 1 DONE:** posted the corrected weekly Slack card — **9 ghosts / 9 team / 1 joiner**. Caught that the auto-linker (exact-name only) had mislabeled **6 members as ghosts** (nickname/spelling gaps: Matt≠Matthew, JJ≠Joshua, Debbie≠Debora, M≠Mitesh, Mari Ashley≠Mari, Tamkin Amin≠Tamkin) + Debbie Macias = Staff → added to `mds_exclude.json`. Those 6 are still **unlinked/unscored** in AT (FB row not linked to member) — a follow-up.

## ✅ Done + verified this session (2026-07-06)
- **Point 1 — FB engagement data current.** Ran `process_fb.py` on `Facebook_Group_Insights_7-06-2026.xlsx` → **768 rows @ 2026-07-06, 95 contributors**. Validated END-TO-END: source xlsx == FB Engagement table (Michael Patrón 15/79) == leaderboard mirror lookup (Mo Kuhail 2/47); `Member's score NEW` is a formula → auto-current.
- **Point 2 — Insights export fixed + guarded (extension v0.46).**
  - **2a (root cause):** `clickInsightsDownload`'s `dlg()` grabbed the FIRST `[role="dialog"]` = the **Messenger chat flyout**, so it searched the wrong dialog and the tick silently no-op'd. Fixed to target the real Download dialog (has the `Growth`/`All` checkbox) + click **All** + a **hard safeguard** that aborts+warns unless Engagement+Members are ticked. **Andy confirms 2a works.**
  - **2b (`auto_import.py`):** before import, `validate_xlsx()` checks the export has Contributors + Daily-numbers tabs; if not → **Slack warn + does NOT import** (can't zero the scores). Tested: 7-06 passes, bad file flagged.
  - **#3 reinforcement:** `auto_import.py` gates reconcile on roster ≥500 → partial roster = **Slack warn + skip reconcile** (prevents a wrong ghost card). `reconcile.py` also guards internally (partial roster → exclude-only ghosts, no departed).
  - Extension roster capture warns loudly if <500 (v0.45).
- **Health freshness fix (NOT deployed).** `mds-digest-web/src/lib/tools-health/fb.ts` made **schedule-aware**: flags a missed Monday run same-day (old `staleDays<=8=healthy` → new `degraded` when this week's scrape didn't land). Proven (old=healthy→new=degraded for the 6-day-stale case). **Still on the `mobile-adaptive` branch, uncommitted — needs deploy.**

## Assets on disk
- `~/Downloads/mds_roster_full (5).json` = **743, COMPLETE, banned excluded** → use this for the fresh ghost list.
- `~/Downloads/Facebook_Group_Insights_7-06-2026.xlsx` = good (All ticked).

## 🔜 Open tasks (ordered — do next session)
1. **Fresh ghost / team / new-joiner list (Andy is waiting on this).** Run `reconcile.py "~/Downloads/mds_roster_full (5).json" --apply` (or dry-run first) → posts the CORRECTED Slack card. Expect ~12 ghosts (like the Jun-30 card), 8 team, joiners from the "New members this week" card. The bad Jul-6 card (3 ghosts) came from the partial 70-roster — this replaces it.
2. **Point 4 — health dashboard (deploy + data-quality):**
   a. **Deploy** the `fb.ts` schedule-aware fix (Andy's branch call: commit just `fb.ts` to `main` + push → Vercel deploys; git author `andy.verdy1@gmail.com`). Verify via the triage endpoint that fb-engagement now reads `degraded` for a missed run.
   b. **Add data-quality checks** so the dashboard catches what freshness misses: flag when FB engagement is **all-zero** (a Growth-only export slipped through), and surface the `auto_import` warnings (partial roster / bad export). "If the file doesn't look right → health check + Slack." (Andy's explicit ask.)
   c. **Point 2a → health signal:** represent Insights-export completeness as a monitored signal.
3. **Point 6 — Claude weekly routine (Andy wants to check all this weekly).** Stand up a scheduled cloud agent (`schedule` skill) OR cron that runs the weekly checks — engagement fresh? roster ≥700? ghost list sane? safeguards green? — and reports to Andy. Decide cadence (Mondays, after the 1am capture).
4. **Point 5 — recompute:** confirm the public leaderboard reflects the fresh 7-06 data on reload (formula auto-updates). If a config-engine "Engagement Score" is also shown, fire the recompute ping (`UCxyzY1RXzrIHtmX`).
5. **Confirm 2a live** on the next real Insights capture (fix is grounded in the live DOM; 1 live confirm pending).

## ⏸ Parked — FB conversation-digest scraper (separate from the scorecard above)
- Capture WORKS on a healthy feed (validated: 12 posts/82 comments, spot-check passed) but **only reaches ~12 of ~58 weekly posts (~21%)** — FB virtualizes the feed; the opener only finds `/posts/` links in rendered comment blocks. **Full coverage needs a redesigned opener, NOT more scroll patches.**
- **FB THROTTLED the feed** after 30+ capture runs over 2 days; verified the feed caps at ~1–4 posts and won't paginate; **3 days of rest did NOT clear it.** Don't run digest capture until it recovers; then ONE slow run, weekly cadence.
- Save safeguards shipped (v0.40→v0.44): page writes `mds_feed.json` + `mds_feed_partial.json` every 30 posts (survives worker death); continuous `localStorage` copy; **"💾 Recover last capture"** button. Extension digest scroll logic reverted to the working v0.42 baseline (v0.43 deep-scroll stalled).
- Next: on a fresh/un-throttled session, human-scroll test whether it's account-wide or tool-flagged, then redesign the opener.
