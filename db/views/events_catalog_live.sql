-- GENERATED by scripts/db_export_schema.py from the live database. Do not hand-edit.
create or replace view digest.events_catalog_live as
 SELECT at_record_id,
    name,
    start_at,
    end_at,
    event_type,
    phase,
    guests_policy,
    chapter_area,
    chapter_hint,
    venue_capacity,
    location,
    city_state,
    member_reg_link,
    guest_reg_link,
    chapter_member_reg_link,
    public_page_url,
    app_event_id,
    app_title,
    app_starts_at,
    app_timezone,
    app_city,
    app_is_public,
    app_url,
    synced_at,
    chapter_ids,
    style,
    embedding,
    thumbnail_url,
    not_a_real_event
   FROM digest.events_catalog
  WHERE not_a_real_event IS NULL;
