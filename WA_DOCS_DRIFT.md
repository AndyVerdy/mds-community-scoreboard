> 📌 **Andy: keep answers short — 1–4 paragraphs** (not too short, not too long). He asks for details if needed. <!-- ANDY-PREF -->

# WA sister-docs — drift audit (2026-07-10)

> Generated by a 13-page parallel audit vs the current matcher/identity reality. **Only 5 of ~30 pages were updated this session (all in the Digest doc); these are the STALE ones in the other pages.**

**8 of 13 pages stale · 35 findings.** Clean: Engagement 06 Team Ops, Approvals 02 Progress, Approvals 07 Centurion Verifier, Approvals 08 WA Outbound Sender, Digest 01 Overview.

⚠️ **CAVEAT — likely FALSE POSITIVES:** the Approvals-doc findings about *Leads / name fallback / 3 phone fields* describe the **join-request triage matcher `ib7g9bBddhzCbj4X`** (a DIFFERENT, unchanged matcher), not `4B79`. Do NOT 'fix' those. Only the Approvals **row count** (4,906 → 5,604) is real.

---

## Engagement 01 Overview
- **[HIGH]** stale: _Daily Match Backfill workflow → ensures new WA Members get matched to MDS Members DB by phone._
  - why: Workflow 4B79OVfyT2a9a3Xt is no longer a one-way 'Match Backfill' (8 nodes). It is now 'MDS WA - Member Matcher (Preferred+Verified, set/clear, dynamic)', 15 nodes: it matches WA phone last-10 against Preferred Phone Num
  - fix: Rename to 'MDS WA - Member Matcher (Preferred+Verified, set/clear, dynamic)' and describe it as a set/clear matcher on Preferred/Verified phone with an 8am sweep + targeted webhook.
- **[MEDIUM]** stale: _NEW Match Backfill workflow populates source_member_id daily_
  - why: The matcher does not just 'populate' source_member_id — it now CLEARS source_member_id (and both link fields) on no-match as part of an all-or-nothing bundle, and runs on a daily sweep + targeted webhook rather than a si
  - fix: Change to 'sets source_member_id on match and clears it on no-match (daily sweep + Verified-number webhook).'
- **[MEDIUM]** stale: _Match Backfill | n8n + Airtable Meta API | Daily, idempotent, separate from Digest_
  - why: Same workflow renamed to the Member Matcher (15 nodes); cadence is now daily 8am sweep + targeted webhook (not just 'Daily'), throttled to 4 req/s, and it writes/clears both link fields.
  - fix: Update the Stack row name and note the sweep+webhook cadence, 4 req/s throttle, and set/clear behavior.
- **[MEDIUM]** stale: _We pull `msgs_30d`, `last_active_at`, `source_member_id` from `Members`_
  - why: The Engagement Sync workflow (v9D1bROMGMivfXH2) was rebuilt 2026-07-02 to resolve each member by PHONE with active-dedup every run and NO LONGER uses source_member_id. (source_member_id is still consumed by the separate 
  - fix: Note that Engagement Sync now resolves members by phone + active-dedup, not by source_member_id.
- **[LOW]** stale: _source_member_id (89%)_
  - why: Stale coverage figure. WA DB Members is now ~515 rows with ~447 matched (~87%), and because the matcher clears source_member_id on no-match there is no longer a fixed ~89% backfill level. (Members DB itself is ~5,604 row
  - fix: Drop or refresh the 89% figure; current match coverage is ~447/515 (~87%).

## Engagement 02 Progress
- **[HIGH]** stale: _`4B79OVfyT2a9a3Xt` | MDS WA - Match Backfill_
  - why: Workflow 4B79OVfyT2a9a3Xt was renamed and re-architected; it is no longer a one-way "Match Backfill" but a set/clear matcher. This name (also repeated as "Match Backfill" in the roadmap checkbox, the match-rate paragraph
  - fix: Rename to "MDS WA - Member Matcher (Preferred+Verified, set/clear, dynamic)" everywhere it appears.
- **[HIGH]** stale: _**377 of 475** WA Members matched to the MDS CRM (**79%**)_
  - why: Live counts as of 2026-07-10 are WA DB Members ~515 rows with ~447 matched (~87%), 0 partial states. The 377/475/79% figures (also in the "Last updated" header) are stale.
  - fix: Update to ~447 of 515 matched (~87%), 0 partial states; Members DB ~5,604 rows.
- **[HIGH]** stale: _Match rate (live 2026-06-09): 377 of 475 WA Members matched_
  - why: The "live" match-rate snapshot in the header line ("match rate 377 of 475 = 79%") is superseded by ~447 of 515 (~87%).
  - fix: Replace the header match-rate figure with ~447 of 515 (~87%).
- **[MEDIUM]** stale: _| `4B79OVfyT2a9a3Xt` | MDS WA - Match Backfill | ✅ | 6 | `0 8 * * *` tz NY | ✅ wired 2026-05-04 | Live, daily 8am ET. |_
  - why: The matcher now has 15 nodes (was 8), not 6, and it fires on TWO triggers — the daily 8am ET full sweep AND a targeted webhook that rematches only members with a WhatsApp Number (Verified) set. The row shows a cron-only 
  - fix: Update to 15 nodes and note both triggers: daily 8am ET full sweep + targeted rematch webhook.
- **[MEDIUM]** stale: _Real-only. ~377 matched members × days._
  - why: The Member Stats table cites ~377 matched members; the matched set is now ~447.
  - fix: Change to ~447 matched members × days.
- **[MEDIUM]** stale: _**Stale** **`source_member_id`** **cleanup** — 2026-05-04 audit found 1 stale FK out of 397. Re-verify against current 377-matched set._
  - why: The matcher now CLEARS source_member_id on no-match (all-or-nothing bundle, 0 partial states), so stale-FK cleanup is now automatic rather than a pending manual task; and "377-matched set" is a stale count (~447).
  - fix: Mark obsolete — matcher auto-clears source_member_id on no-match; drop the pending task or reframe as verified-clean.
- **[MEDIUM]** stale: _`v9D1bROMGMivfXH2` | MDS Members - WA Engagement Sync | ✅ | 5 | `0 9 * * *` tz NY | ✅ wired 2026-05-04 | FIXED 2026-05-04: `batchInterval=600ms`, `onError=conti_
  - why: Engagement Sync was rebuilt 2026-07-02: it now resolves each member by PHONE + active-dedup every run and NO LONGER uses source_member_id. The 2026-05-04 fix note predates the rebuild and omits known issue #46 — the sync
  - fix: Note the 2026-07-02 rebuild (phone + active-dedup, no source_member_id) and flag issue #46: sync vs matcher can disagree because sync still uses Leads phone.
- **[MEDIUM]** stale: _Match rate dropped 88%→79% | Andy + Claude | OPEN 2026-06-09 — total grew faster than matching; Match Backfill closing it_
  - why: Match rate is now ~87% (~447/515) and the matcher was redesigned (Preferred + Verified phone, set/clear); this open question frames an outdated problem and workflow name.
  - fix: Close/update — match rate ~87% under the rebuilt Preferred+Verified matcher.
- **[LOW]** stale: _**Stale** **`source_member_id`** **foreign keys** — AT batch PATCH fails the whole 10-record batch when ANY one ID is invalid. Mitigated by `onError: continueRe_
  - why: This risk is now largely mitigated at the source because the matcher clears source_member_id on no-match (0 partial/stale states), rather than relying only on the Engagement Sync PATCH error-handling.
  - fix: Note that matcher set/clear now prevents stale source_member_id FKs at the source.

## Engagement 04 Stack
- **[HIGH]** stale: _MDS WA - Match Backfill_
  - why: Workflow 4B79OVfyT2a9a3Xt has been renamed and rescoped; the 'Match Backfill' identity no longer exists.
  - fix: Rename to 'MDS WA - Member Matcher (Preferred+Verified, set/clear, dynamic)'.
- **[HIGH]** stale: _with the proven Approvals matcher formula (OR phone last-10 across 3 fields + name fallback)_
  - why: The matcher now matches WA phone last-10 only against Members-DB Preferred Phone Number OR WhatsApp Number (Verified); the Leads phone (the 3rd field) is DROPPED and the name fallback is gone.
  - fix: Change to 'phone last-10 vs Preferred Phone Number OR WhatsApp Number (Verified) — Leads phone dropped, no name fallback'.
- **[HIGH]** stale: _`PATCH WA Member` (HTTP PATCH) — write `source_member_id` + `source_member_link` to WA Members. BatchSize 5, batchInterval 250ms._
  - why: The matcher now SETS on match and CLEARS on no-match (all-or-nothing bundle, 0 partial states), owns BOTH links — the WA 'Member' link AND the reverse Members-DB 'Whatsapp Channels Sync' link — and is throttled to 4 req/
  - fix: Describe set-on-match / clear-on-no-match writing both the WA 'Member' link and the reverse Members-DB 'Whatsapp Channels Sync' link, throttled 4 req/s.
- **[HIGH]** stale: _`List Matched WA Members` (Airtable search) — `tbli8B589iNbsGF0Z` filter `AND({source_member_id}!='', {phone}!='')`_
  - why: Engagement Sync was rebuilt 2026-07-02 to resolve each member by PHONE with active-dedup every run and NO LONGER uses source_member_id, so it does not gate on a matched source_member_id anymore.
  - fix: Rewrite node list to reflect phone-based resolution + active-dedup every run; remove the source_member_id filter dependency.
- **[MEDIUM]** stale: _| `4B79OVfyT2a9a3Xt` | MDS WA - Match Backfill | ✅ | 6 | `0 8 * * *` tz NY = 8am ET daily |_
  - why: Node count is now 15 (not 6), and the trigger is a daily 8am FULL sweep PLUS a targeted webhook that rematches only members with WhatsApp Number (Verified) set; the single-cron description is incomplete.
  - fix: Update to 15 nodes and add the targeted webhook trigger alongside the 8am ET full-sweep cron.
- **[MEDIUM]** stale: _**Status:** built, batch fix shipped 2026-04-27. Pending manual test + activation._
  - why: Engagement Sync (v9D1bROMGMivfXH2) was rebuilt 2026-07-02 and runs in production every cycle (it is part of live known-issue #46), so it is active — not pending activation. The table's '❌ / 5 nodes' row is likewise stale
  - fix: Mark Engagement Sync active and note the 2026-07-02 phone-resolution rebuild; drop 'pending activation'.
- **[MEDIUM]** stale: _5 newly matched. 376 → 381 of 432 (88%)_
  - why: WA DB Members now has 515 rows with ~447 matched and 0 partial states; the 381-of-432 figures are outdated.
  - fix: Update counts to ~447 matched of 515 WA Members (0 partial).
- **[LOW]** stale: _Until ~2026-05-23, expect heavy Silent/Dormant skew._
  - why: That calibration window has already passed (current date 2026-07-10), so the caveat about waiting until 2026-05-23 is expired.
  - fix: Remove the pre-2026-05-23 caveat or replace with current tier-threshold status.

## Engagement 05 QA
- **[HIGH]** stale: _Smoke test — Match Backfill_
  - why: Workflow 4B79OVfyT2a9a3Xt is no longer named "Match Backfill" and is no longer a one-way backfill. It is now "MDS WA - Member Matcher (Preferred+Verified, set/clear, dynamic)" (15 nodes, was 8), which SETS on match and C
  - fix: Rename the section to "Smoke test — Member Matcher" and describe the set-on-match / clear-on-no-match all-or-nothing behavior.
- **[HIGH]** stale: _51 of 432 WA Members can't match_
  - why: Counts are stale and the matcher identity changed. WA DB Members is now 515 rows with ~447 matched and 0 partial states (~68 unmatched), not 432 total / 51 unmatched. The 8-LID / 1-Twilio / 42-real-phone breakdown that s
  - fix: Update to ~515 WA Members / ~447 matched / ~68 unmatched, 0 partial, and re-derive the unmatched breakdown against the Preferred+Verified matcher.
- **[MEDIUM]** stale: _Verify count of unmatched WA Members BEFORE - AFTER = newly matched_
  - why: This step assumes a backfill that only ever reduces the unmatched count. The matcher now SETS on match and CLEARS on no-match (all-or-nothing bundle, 0 partial states), so unmatched can increase as well as decrease and a
  - fix: Verify both directions: newly SET matches and newly CLEARED no-matches (plus the reverse Members-DB link), rather than a monotonic unmatched delta.
- **[LOW]** stale: _batch endpoint is fast — 39 calls × ~250ms_
  - why: The "39 calls" figure is derived from the old 381-record/÷10 batched-PATCH architecture. Engagement Sync v9D1bROMGMivfXH2 was rebuilt 2026-07-02 to resolve each member by phone with active-dedup every run (no longer usin
  - fix: Recompute the call count from the current active-member set post-2026-07-02 rebuild, or drop the specific number.

## Approvals 01 Overview
- **[HIGH]** stale: _**Phone (fallback):** `Phone Number (from Link to Member Leads)` (`fldbY9PNL8trrwcmM`) — lookup on linked Lead record_
  - why: The matcher (4B79OVfyT2a9a3Xt) now DROPS the Leads phone entirely — it matches only WA phone last-10 vs Members-DB Preferred Phone Number OR WhatsApp Number (Verified). This is exactly the field that Engagement Sync stil
  - fix: Delete the Leads-phone fallback; matching is Preferred Phone Number OR WhatsApp Number (Verified) only.
- **[MEDIUM]** stale: _**Phone (primary):** `Preferred Phone Number` (`fldLx5ngxaR2gU0Ir`) — E.164 normalized_
  - why: Two drifts: (1) matching now compares last-10 digits, not full E.164; (2) it omits the now-primary editable field WhatsApp Number (Verified), which drives matching and auto-fires the matcher webhook via automation wflw0e
  - fix: Rewrite as 'last-10-digit match on Preferred Phone Number OR WhatsApp Number (Verified)'.
- **[MEDIUM]** stale: _**Name (fallback):** `Full Name` formula (`fldYkS0TgeMAtIrU8`) — exact first, then fuzzy >0.85_
  - why: The current matcher is phone-only (last-10 vs Preferred Phone Number / WhatsApp Number (Verified)); it no longer does exact-or-fuzzy name matching. The related 'Phone-less name match -> downgrade to REVIEW' note is likew
  - fix: Remove the name/fuzzy fallback and the phone-less-name REVIEW downgrade; matching is phone-only.

## Approvals 04 Stack
- **[MEDIUM]** stale: _Base: `appou5JVr0WIrioWS` · Table: `tblfwOSROSHfuYUxv` · **4,906 rows** (verified 2026-04-26)_
  - why: This is the shared Members DB (base appou5JVr0WIrioWS). Per current truth (verified 2026-07-10) it now holds ~5,604 rows; 4,906 is a stale April snapshot.
  - fix: Update the Members table row count to ~5,604 rows (verified 2026-07-10).
- **[MEDIUM]** stale: _`Preferred Phone Number`, `WhatsApp Number`, `Phone Number (from Link to Member Leads)`_
  - why: In the current Members DB, `WhatsApp Number` is now a LOOKUP of the WA phone (formerly editable text), and the editable field that actually drives matching is `WhatsApp Number (Verified)` — which this field list omits en
  - fix: Add `WhatsApp Number (Verified)` as the editable matching driver and mark `WhatsApp Number` as a lookup (no longer an editable member-entered phone).

## Approvals 05 QA
- **[MEDIUM]** stale: _`Preferred Phone Number` empty on a valid member → relies on `WhatsApp Number` or Link-to-Leads fallback_
  - why: The current identity model dropped the Leads phone entirely ("LEADS PHONE IS DROPPED"), so the Link-to-Leads fallback no longer exists; and Members-DB "WhatsApp Number" is now a read-only LOOKUP of the matched WA phone (
  - fix: Change fallback to `WhatsApp Number (Verified)` and remove the Link-to-Leads fallback (Leads phone is no longer used for matching).

## Digest 06 Team Ops
- **[HIGH]** stale: _~491 rows; about 90% are now linked to the MDS members database by phone number_
  - why: Current live counts for the WA DB Members table are ~515 rows with ~447 matched (~87%). '~491 rows' is explicitly a stale figure per the current truth.
  - fix: Change to '~515 rows; ~447 (about 87%) are linked to the MDS members database by phone number'.
- **[HIGH]** stale: _49 of them right now — under review_
  - why: With 515 rows and ~447 matched, there are ~68 unmatched (empty Member) rows, not 49. The '49 unmatched' figure is explicitly called out as stale.
  - fix: Change to '~68 of them right now' (515 minus ~447 matched).
- **[HIGH]** stale: _It's built but **turned OFF** until member-matching review is complete — so today, sign-in is not gated by membership status._
  - why: Access-gating is now LIVE (in lenient mode — it denies only Removed/Declined statuses), so sign-in IS now gated by membership status. This callout still says it is turned off.
  - fix: Change to: access-gating is now LIVE in lenient mode — sign-in is denied only for members whose AT Database Status is Removed or Declined.
